import { style, fontFace } from "@vanilla-extract/css";
import { vars } from "./theme.css";

const Moistyle = style({

})

const Header = style({

})

const Competences = style({

})

export {Moistyle, Header, Competences};